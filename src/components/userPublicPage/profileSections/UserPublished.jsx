import React from 'react';

function UserPublished({ isUserPage }) {
  return (
    <div>
      This is is the userPage to display all user posted images, and unposted as
      of yet posts.
    </div>
  );
}

export default UserPublished;
